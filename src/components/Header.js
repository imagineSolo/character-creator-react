import React from "react";
import styles from "./Header.module.scss";

const header = () => {
  return <header className={styles.Header}>Fantasy Character Creator</header>;
};

export default header;
